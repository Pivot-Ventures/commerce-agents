{
  "name": "acme-examples",
  "private": true,
  "workspaces": [
    "web-shared",
    "*/storefront-web",
    "*/merchant-web",
    "*/admin-web"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  }
}
